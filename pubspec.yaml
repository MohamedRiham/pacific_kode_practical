name: pacific_kode_practical
description: "allows users to view jobs and apply."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  connectivity_plus:
  http:
  shared_preferences:
  provider:
  hive_local_storage:
  email_validator:
  get:
dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  flutter_lints: ^5.0.0
  hive_generator:

flutter:

  uses-material-design: true

  assets:
    - assets/images/
